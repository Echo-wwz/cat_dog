# ---------------------------------------------------------------
# 猫狗分类实验 · Git 忽略规则
#
# 原则：仓库只装「代码 + 数据 + 文档 + 实验结果」，
#       「模型权重」和「缓存」不入库。
#
# 为什么不传权重：
#   1. GitHub 单文件硬上限 100 MB，本项目 .pth 有 178~233 MB，直接推不上去；
#      用 Git LFS 能推，但 795 MB 会吃掉免费额度（1 GB）的 78%。
#   2. 权重几分钟就能重新训练生成（run_all.py），没必要占仓库体积。
#   3. 预训练权重由 config.py 首次运行时自动下载到 torch_cache/。
# ---------------------------------------------------------------

# ---- 模型权重：不入库 ----
*.pth
*.pt
*.ckpt
*.safetensors

# 预训练权重缓存（config.py 自动指向这里，首次运行联网重下）
torch_cache/

# ---- Python 缓存 ----
__pycache__/
*.py[cod]
*$py.class
*.so

# ---- 虚拟环境 ----
.venv/
venv/
env/

# ---- 编辑器 / 系统 ----
.idea/
*.swp
.DS_Store
Thumbs.db

# ---- 临时脚本 / 手动副本（不入库）----
_smoke.py
_diag_*.py
_diag_*.out
*副本*

# 注意：outputs/ 下的结果数据要提交，所以这里只忽略 .pth（见上方规则），
#      不整目录忽略 outputs/。保留的文件：
#        outputs/*.csv / *.json   ← 消融与交叉验证的结构化结果
#        outputs/*.txt            ← txt 划分索引
#        outputs/logs/            ← 用 run_all.py 跑时产生的完整日志
