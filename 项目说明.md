# 猫狗分类实验 · 项目说明

> 一页纸看懂：**环境 → 路径 → 任务 → 产物 → 命令**。
> 原始教学文档：`D:\cxdownload\实验文档\猫狗分类实验指导书.md`
> 详细操作手册：[`README.md`](README.md) · 结果分析：[`实验结果报告.html`](实验结果报告.html)

---

## 一、项目环境

### 1.1 硬件（本机实测）

| 项目 | 值 |
|---|---|
| 主机名 | `Echo-Y7000P` |
| 机型 | Lenovo 83F3（游戏本） |
| 操作系统 | Windows 11（内核 10.0.26200） |
| CPU | Intel Core Ultra 7 251HX，18 核 / 18 线程 |
| 内存 | 15.4 GB 物理（DDR5-6400 单条，单通道） |
| **GPU** | **NVIDIA GeForce RTX 5070 Laptop GPU**，显存 **11.9 GB**，算力 12.0（Blackwell） |
| 磁盘 | YMTC 954 GB + Predator GM7 2 TB |

> ⚠️ 查显存只信 `nvidia-smi` 或 `torch.cuda.get_device_properties()`。
> Windows 的 `Win32_VideoController.AdapterRAM` 报 4GB 是 32 位整数溢出的假象。

### 1.2 解释器（**本项目的关键**）

```
C:\Users\Echo\.conda\envs\dl\python.exe
```

**必须用这个解释器**。anaconda base（`D:\anaconda3\python.exe`，Python 3.14.6）**没有 torch**，
且 Python 3.14 目前拿不到 torch 的轮子。

| 项目 | 值 |
|---|---|
| conda 环境名 | `dl` |
| Python | 3.12.14 |
| 激活方式 | `conda activate dl` |

### 1.3 依赖版本（已实测通过）

| 包 | 版本 | 用途 |
|---|---|---|
| torch | **2.13.0+cu130** | 深度学习框架，CUDA 13.0 |
| torchvision | 0.28.0+cu130 | 预训练模型 / transforms |
| scikit-learn | 1.9.0 | accuracy / precision / recall / F1 |
| Pillow | 12.3.0 | 图片读取 |
| numpy | 2.5.2 | 折划分、随机数 |
| gradio | **6.27.0** | 第六步拍照小程序 |

`CUDA 可用: True` · 设备自动选择：有 GPU 用 GPU，`--device cpu` 可强制 CPU。

### 1.4 项目内路径（**数据与权重都在项目里**）

| 用途 | 绝对路径 | 说明 |
|---|---|---|
| **数据集** | `D:\CV\cat_dog\data\` | 100 张，`cat.1.jpg`~`cat.50.jpg` / `dog.51.jpg`~`dog.100.jpg` |
| **权重缓存** | `D:\CV\cat_dog\torch_cache\hub\checkpoints\` | 4 个 `.pth`，**`config.py` 自动指向** |
| 输出产物 | `D:\CV\cat_dog\outputs\` | 权重 / CSV / JSON / 日志，自动创建 |
| 原始教学文档 | `D:\cxdownload\实验文档\猫狗分类实验指导书.md` | |

权重缓存内容（已就位）：

| 文件 | 大小 | 用在哪 |
|---|---|---|
| `alexnet-owt-7be5be79.pth` | 244.4 MB | 第一 / 二步 |
| `resnet18-f37072fd.pth` | 46.8 MB | 第三 / 四步 |
| `mobilenet_v3_small-047dcff4.pth` | 10.3 MB | 第四步 |
| `shufflenetv2_x1-5666bf0f80.pth` | 9.2 MB | 第四步 |

> **因为数据和权重都在项目内，整个 `D:\CV\cat_dog` 文件夹可以拷到别的机器直接跑，不用重新下载。**

### 1.5 环境变量

| 变量 | 作用 | 默认 |
|---|---|---|
| `CATDOG_DATA` | 换数据集目录 | 不用设（自动用 `data\`） |
| `CATDOG_TORCH_HOME` | 覆盖权重缓存位置 | 不用设（自动用 `torch_cache\`） |
| `TORCH_HOME` | torch 通用缓存位置 | 由 `config.py` 自动设为 `torch_cache\` |

数据集自动探测顺序（全部绝对路径，**和当前工作目录无关**）：
`CATDOG_DATA` → `cat_dog\data\` → `cat_dog\datasets\`

**只用 CPU 跑**：所有脚本加 `--device cpu`，AlexNet 30 轮会很慢，建议配 `--epochs 5`。
**显存不足**：调小 `--batch-size`（默认 16，可降到 8 或 4）。

### 1.6 权重缓存位置（已经自动设好了）

`config.py` 启动时会做这件事（在 `import torch` **之前**）：

```
若 torch_cache\hub\checkpoints\ 存在  ->  os.environ["TORCH_HOME"] = <项目根>\torch_cache
若设了 CATDOG_TORCH_HOME              ->  优先用它
```

所以**什么都不用配**。跑 `python check_env.py` 可看到实际生效目录：

```
权重缓存    : D:\CV\cat_dog\torch_cache\hub\checkpoints
  来源      : 环境变量 TORCH_HOME=D:\CV\cat_dog\torch_cache
  [有] alexnet-owt-7be5be79.pth                244.4 MB  alexnet（第一/二步）
  ...
```

想临时换到别处，设 `CATDOG_TORCH_HOME` 即可（**注意路径差一层 `hub`**）：

| 方式 | checkpoints 实际落在 |
|---|---|
| `CATDOG_TORCH_HOME=D:\别的盘\torch_cache` | `D:\别的盘\torch_cache\hub\checkpoints\` |
| `TORCH_HOME=D:\别的盘\torch_cache` | `D:\别的盘\torch_cache\hub\checkpoints\` |
| 代码里 `torch.hub.set_dir(r"D:\别的盘\torch_cache")` | `D:\别的盘\torch_cache\checkpoints\` ← **少一层** |

> ⚠️ 本项目布局**带 `hub` 层**，所以必须用 `TORCH_HOME` / `CATDOG_TORCH_HOME`，
> **不要**用 `torch.hub.set_dir()`。

优先级（实测）：`CATDOG_TORCH_HOME` > 项目内 `torch_cache` > `TORCH_HOME` > `XDG_CACHE_HOME` > `~/.cache`。

---

## 二、代码路径

### 2.1 项目根目录

```
D:\CV\cat_dog
```

### 2.2 目录树

```
D:\CV\cat_dog\
├── run_all.py                 ← 总控入口，一条命令跑完全部
├── config.py                  ← 全局配置（数据 / 权重缓存 / 超参 / 设备）
├── check_env.py               ← 环境自检
├── common.py                  ← 公共代码（指导书 0.2 + 0.3）
├── models_manual.py           ← 手写 ResNet18
├── step1_scratch.py           ← 第一步
├── step2_pretrained.py        ← 第二步
├── step3_augment.py           ← 第三步
├── step4_compare.py           ← 第四步
├── step5_train_save.py        ← 第五步上
├── step5_predict.py           ← 第五步下
├── step6_app.py               ← 第六步
├── step7_ablation.py          ← 加餐①：多随机种子消融
├── step8_crossval.py          ← 加餐②：5 折交叉验证
├── _diag_rng.py               ← 随机性诊断
├── README.md                  ← 操作手册
├── 项目说明.md                 ← 本文档
├── 实验结果报告.html            ← 结果分析（可打印）
├── data\                      ← 数据集（100 张，项目内）
├── torch_cache\               ← 权重缓存（项目内）
│   └── hub\checkpoints\       ←   ← 4 个 .pth（注意多一层 hub）
└── outputs\                   ← 全部产物（自动创建）
    ├── *.pth                  ← 模型权重
    ├── *.csv / *.json         ← 结构化实验结果
    ├── trainDatasets.txt / testDatasets.txt
    └── logs\                  ← 每个任务的完整日志
```

### 2.3 文件清单（含行数与作用）

| # | 文件 | 绝对路径 | 行数 | 作用 |
|---|---|---|---|---|
| 1 | `run_all.py` | `D:\CV\cat_dog\run_all.py` | 182 | **总控脚本**：15 个任务分组编排，输出同时落盘，结尾汇总表 |
| 2 | `config.py` | `D:\CV\cat_dog\config.py` | 78 | 全局配置：数据目录 / **权重缓存自动指向** / `SEED` / `BATCH_SIZE` / `DEVICE` / `set_seed()` |
| 3 | `check_env.py` | `D:\CV\cat_dog\check_env.py` | 103 | 自检依赖、GPU、数据集张数、**权重缓存位置与四个权重是否齐全** |
| 4 | `common.py` | `D:\CV\cat_dog\common.py` | 170 | `FileNameDataset` / `get_loaders` / `train_and_eval` / `evaluate` |
| 5 | `models_manual.py` | `D:\CV\cat_dog\models_manual.py` | 114 | 手写 `BasicBlock` + `ResNet18Manual` + 预训练加载 |
| 6 | `step1_scratch.py` | `D:\CV\cat_dog\step1_scratch.py` | 212 | 第一步：AlexNet 从零训练；含指导书原版 txt 索引划分（两种划分**分文件保存**） |
| 7 | `step2_pretrained.py` | `D:\CV\cat_dog\step2_pretrained.py` | 117 | 第二步：ImageNet 预训练 + 冻结 / 全解冻微调 |
| 8 | `step3_augment.py` | `D:\CV\cat_dog\step3_augment.py` | 90 | 第三步：数据增强；含 `--no-augment` / `--random-erasing` |
| 9 | `step4_compare.py` | `D:\CV\cat_dog\step4_compare.py` | 131 | 第四步：resnet18 / mobilenet / shufflenet / efficientnet 对比 |
| 10 | `step5_train_save.py` | `D:\CV\cat_dog\step5_train_save.py` | 93 | 第五步上：按验证集最佳保存 `best_model.pth`；换模型时提醒改名 |
| 11 | `step5_predict.py` | `D:\CV\cat_dog\step5_predict.py` | 139 | 第五步下：单张 / `--dir` 批量推理；`--model` 支持 6 种架构（含两种 AlexNet） |
| 12 | `step6_app.py` | `D:\CV\cat_dog\step6_app.py` | 105 | 第六步：Gradio 摄像头小程序 |
| 13 | `step7_ablation.py` | `D:\CV\cat_dog\step7_ablation.py` | 241 | 加餐①：10 配置 × 5 种子的 mean ± std + 配对比较 |
| 14 | `step8_crossval.py` | `D:\CV\cat_dog\step8_crossval.py` | 215 | 加餐②：分层 5 折交叉验证，池化 100 张 |
| 15 | `_diag_rng.py` | `D:\CV\cat_dog\_diag_rng.py` | 57 | 诊断"每轮验证会改变 shuffle 顺序" |
| 16 | `README.md` | `D:\CV\cat_dog\README.md` | 410 | 操作手册（怎么跑 / 每步在做什么 / 9 处坑 / FAQ） |
| 17 | `实验结果报告.html` | `D:\CV\cat_dog\实验结果报告.html` | 781 | 结果分析报告（深色，带打印样式） |

> 全部 `.py` 已通过 `py_compile` 编译检查。

---

## 三、任务清单

### 3.1 总控脚本用法

```bash
cd "D:/CV/cat_dog"
PY="C:/Users/Echo/.conda/envs/dl/python.exe"

$PY check_env.py               # ⓪ 环境 + 数据集 + 权重缓存自检
$PY run_all.py --dry-run       # 只打印计划，不执行（先看这个）
$PY run_all.py --list          # 列出全部任务编号
$PY run_all.py single          # 组：六步单次运行
$PY run_all.py ablation        # 组：多随机种子消融
$PY run_all.py crossval        # 组：5 折交叉验证
$PY run_all.py app             # 组：Gradio 小程序（前台阻塞）
$PY run_all.py all             # single + ablation + crossval
$PY run_all.py single --only 1 4       # 只跑指定编号
```

行为约定：

- 每个任务的 stdout **同时打到屏幕并写入** `outputs\logs\<编号>_<名称>.log`
- 某个任务失败**不中断**后续任务
- 结尾打印"成功 / 失败 / 耗时"汇总表

### 3.2 任务总表（共 15 个）

#### 组 `single` —— 六步单次运行（seed=42，合计约 4 分钟）

| 编号 | 任务 | 执行的命令 | 预估 | 参考结果 |
|---|---|---|---|---|
| `0` | 环境自检 | `check_env.py` | 10 秒 | 通过 |
| `1` | 第一步：AlexNet 从零训练 30 轮 | `step1_scratch.py` | 40 秒 | **0.80** |
| `1b` | 第一步：指导书原版 txt 索引划分 | `step1_scratch.py --split txt` | 40 秒 | **0.75** |
| `2` | 第二步：预训练冻结，30 轮 | `step2_pretrained.py` | 70 秒 | **0.95** |
| `2b` | 第二步进阶：全解冻微调 5 轮 | `step2_pretrained.py --finetune --epochs 5` | 20 秒 | **0.95** |
| `3` | 第三步：数据增强 5 轮 | `step3_augment.py` | 15 秒 | **0.90** |
| `3b` | 第三步对照组：关掉增强 | `step3_augment.py --no-augment` | 15 秒 | **0.90** |
| `4` | 第四步：三模型横向对比 | `step4_compare.py` | 30 秒 | **0.90 / 0.90 / 0.75** |
| `5a` | 第五步：训练并保存 best | `step5_train_save.py` | 20 秒 | best **0.90** |
| `5b` | 第五步：全量 100 张推理 | `step5_predict.py --dir <数据集>` | 20 秒 | 全量 **91%** |

#### 组 `ablation` —— 多随机种子消融（合计约 6 分钟）

| 编号 | 任务 | 执行的命令 | 预估 | 状态 |
|---|---|---|---|---|
| `7a` | 消融主矩阵：7 配置 × 5 种子 | `step7_ablation.py --out outputs/ablation_results.csv` | 3 分钟 | 可跑 |
| `7b` | 消融补充：4 配置 × 5 种子 | `step7_ablation.py --configs 2_alexnet_frozen 8_alexnet_finetune 9_resnet18_noaug_15 10_resnet18_aug_15 --out outputs/ablation_extra.csv` | 2 分钟 | 可跑 |
| `7c` | 随机性诊断 | `_diag_rng.py` | 1 分钟 | 可跑 |

#### 组 `crossval` —— 5 折交叉验证（约 9 分钟）

| 编号 | 任务 | 执行的命令 | 预估 | 状态 |
|---|---|---|---|---|
| `8` | 6 配置 × 3 种子 × 5 折 = 90 次训练 | `step8_crossval.py` | 9 分钟 | ✅ **已跑完** |

> 池化后验证样本 100 张，分辨率 **1%**（单次划分只有 20 张 / 5%）。
> 本机实测（`outputs\crossval_results.csv`）：

| 配置 | 池化准确率 | ± std | 逐种子 | 秒/折 |
|---|---|---|---|---|
| 从零训练 AlexNet | 0.6533 | 0.0473 | .69/.67/.60 | 54.5 |
| **AlexNet 预训练冻结** | **0.9233** | 0.0252 | .92/.95/.90 | 10.9 |
| ResNet18 无增强 | 0.9133 | 0.0115 | .92/.90/.92 | 10.0 |
| ResNet18 + 增强 | 0.9100 | 0.0100 | .92/.91/.90 | 14.3 |
| MobileNetV3-S + 增强 | 0.9100 | 0.0100 | .92/.90/.91 | 13.9 |
| ShuffleNetV2 + 增强 | 0.9033 | 0.0058 | .90/.91/.90 | 14.2 |

配对比较：迁移学习 **+0.2700 ± 0.0361（3/3，显著）**；
数据增强 −0.0033（1/3）、MobileNet vs ResNet18 0.0000（1/3）、
ShuffleNet vs ResNet18 −0.0067（0/3）—— 均在噪声内。

> ⚠️ **池化结果推翻了单次划分下"ShuffleNet 明显更差"的结论**：
> 0.74 vs 0.88（差 0.14）→ 实际 0.9033 vs 0.9133（**只差 1 张图**）。
> 四个预训练模型全部落在 0.90–0.92，**模型选型在这份数据上没有意义**。详见报告 §6.3。

#### 组 `app` —— 部署

| 编号 | 任务 | 执行的命令 | 说明 |
|---|---|---|---|
| `6` | Gradio 拍照小程序 | `step6_app.py` | 前台阻塞，`Ctrl+C` 退出 |

启动后：本机 `http://127.0.0.1:7860`；手机同 Wi-Fi 访问 `http://<电脑IP>:7860`（脚本会打印真实局域网 IP）。

### 3.3 单独跑某个脚本时的常用参数

所有脚本都支持 `--epochs / --lr / --batch-size / --device / --seed / --data-dir`，
`python stepX.py --help` 看全部。

| 想做什么 | 命令 |
|---|---|
| 换数据集 | `step1_scratch.py --data-dir "D:\别的\数据集"` |
| 强制 CPU | 任意脚本加 `--device cpu` |
| 快速冒烟 | 加 `--epochs 2` |
| 换随机种子 | 加 `--seed 123` |
| 指导书原版划分 | `step1_scratch.py --split txt` |
| 微调而非冻结 | `step2_pretrained.py --finetune` |
| 加 RandomErasing | `step3_augment.py --random-erasing` |
| 批量推理整个文件夹 | `step5_predict.py --dir "data"` |
| 推理 AlexNet 从零版 | `step5_predict.py outputs/step1_alexnet_scratch_txt.pth data/cat.1.jpg --model alexnet_scratch` |
| 推理 AlexNet 预训练版 | `step5_predict.py outputs/step2_alexnet_frozen.pth data/cat.1.jpg --model alexnet` |
| 自定义消融配置 | `step7_ablation.py --configs 4_resnet18_aug 3_resnet18_noaug --seeds 42 1 2` |

---

## 四、输出产物路径

全部在 `D:\CV\cat_dog\outputs\` 下，**自动创建**。本机已跑完全部任务，产物如下。

### 4.1 模型权重

| 文件 | 大小 | 来源 |
|---|---|---|
| `best_model.pth` | 43 MB | `step5_train_save.py`（第六步小程序加载它） |
| `model_resnet18.pth` | 43 MB | `step4_compare.py` |
| `model_mobilenet_v3_small.pth` | 6 MB | `step4_compare.py` |
| `model_shufflenet_v2_x1_0.pth` | 5 MB | `step4_compare.py` |
| `step1_alexnet_scratch.pth` | 178 MB | `step1_scratch.py`（**默认划分**）—— ⚠️ **当前缺失**，见下方说明 |
| `step1_alexnet_scratch_txt.pth` | 178 MB | `step1_scratch.py --split txt`（指导书原版划分，实测 0.75） |
| `step2_alexnet_frozen.pth` | 217 MB | `step2_pretrained.py` |
| `step2_alexnet_finetune.pth` | 217 MB | `step2_pretrained.py --finetune` |
| `step3_resnet18_aug.pth` | 43 MB | `step3_augment.py` |
| `step3_resnet18_noaug.pth` | 43 MB | `step3_augment.py --no-augment` |

> ⚠️ **`step1_alexnet_scratch.pth`（默认划分）当前不存在。**
> 早期版本两种划分写同一个文件名，跑任务 `1` 再跑 `1b` 时被 txt 版覆盖了，
> 那个文件现已按新规则改名为 `step1_alexnet_scratch_txt.pth`。
> 补跑一次即可生成：`run_all.py single --only 1`（约 40 秒，默认划分，期望 0.80）。
> **本报告与交叉验证的所有数字不受影响**——它们由 `step7`/`step8` 内部独立训练得出，
> 不读这个权重文件。

### 4.2 结构化实验数据

| 文件 | 内容 |
|---|---|
| `ablation_results.csv` / `.json` | 7 配置 × 5 种子：mean / std / min / max / 逐种子准确率 |
| `ablation_extra.csv` / `.json` | 4 配置 × 5 种子：轮数对等与长轮数对照 |
| `crossval_results.csv` / `.json` | 6 配置 × 3 种子 × 5 折：池化准确率 / 各折准确率 / 配对比较 |

### 4.3 其他

| 路径 | 内容 |
|---|---|
| `outputs\logs\` | 每个任务的完整 stdout，命名 `<编号>_<名称>.log` |
| `outputs\trainDatasets.txt` / `testDatasets.txt` | `--split txt` 时生成的索引（每行 `绝对路径 标签`） |

---

## 五、快速上手（复制粘贴即可）

```bash
cd "D:/CV/cat_dog"
PY="C:/Users/Echo/.conda/envs/dl/python.exe"

$PY check_env.py               # ① 先确认环境 / 数据 / 权重都没问题
$PY run_all.py --dry-run       # ② 看一遍要跑什么
$PY run_all.py single          # ③ 跑完六步，约 4 分钟
$PY run_all.py ablation        # ④ 跑消融，约 6 分钟
$PY run_all.py crossval        # ⑤ 跑交叉验证，约 9 分钟
$PY run_all.py app             # ⑥ 启动拍照小程序
```

跑完看这两份：

- `README.md` —— 怎么用、每个脚本的参数、常见问题
- `实验结果报告.html` —— 已实测的结论（浏览器打开，`Ctrl+P` 可导出 PDF）

---

## 六、注意事项

### 6.1 随机性约定（改代码前必读）

`step1`~`step8` 全部遵守同一条顺序，**同一个 seed 才能跑出同一个结果**：

```
set_seed(seed)  ->  构建模型  ->  构建 DataLoader  ->  训练（每轮验证一次）
```

三条容易踩的规则：

1. **`train_and_eval()` 不自己播种**，调用方负责在构建模型前 `set_seed(seed)`。
2. **构建模型要在构建 DataLoader 之前**。`nn.init.xavier_uniform_` 和 DataLoader 的
   shuffle 都消耗全局随机数，顺序不同会让"同一个 seed"跑出不同结果。
3. **每轮必须验证一次**。DataLoader 的每个迭代器都会用全局随机数生成 `base_seed`，
   少迭代一次（比如训练循环里不跑验证），从第 2 轮起 shuffle 顺序就会分叉——
   实测同一个 seed 会跑出 0.80 vs 0.75。

**自检方法**：消融矩阵里 `seed=42` 那一格，应该**精确等于**该脚本单独跑的结果。
当前对齐状态：`1→0.80、2→0.95、3→0.90、4→0.90、6→0.90、7→0.75`

### 6.2 统计读数（写报告时注意）

**两套分辨率，别混用**：

| 方法 | 验证样本 | 分辨率 | 用途 |
|---|---|---|---|
| 单次划分（80/20） | 20 张 | **5%**（1 样本 = 5%） | 六步实验、消融主表 |
| **5 折池化** | 100 张 | **1%**（1 样本 = 1%） | **最终结论以此为准** |

单次划分下各配置的种子间标准差就有 0.05~0.10，所以**差值小于 2 个样本（0.10）时不应下结论**，
只能写"未观察到差异"。

**已实测的全部对照**：

| 对照 | 单次划分（5 种子） | 5 折池化（3 种子） | 判定 |
|---|---|---|---|
| **迁移学习** | **+0.2300 ± 0.1151（5/5）** | **+0.2700 ± 0.0361（3/3）** | ✅ **显著** |
| 数据增强 | −0.0200（0/5） | −0.0033（1/3） | 噪声内（方向为负） |
| RandomErasing | +0.0100（1/5） | — | 噪声内 |
| 冻结 vs 微调 | +0.0500（3/5） | — | 噪声内 |
| 数据增强 @15 轮 | −0.0100（1/5） | — | 噪声内 |
| MobileNet vs ResNet18 | — | 0.0000（1/3） | 完全一致 |
| ShuffleNet vs ResNet18 | — | −0.0067（0/3） | 略低，幅度极小 |

**结论**：唯一站得住的是**迁移学习**。数据增强在本数据上三次方向一致都是负的
（不是"有效但测不出"，是"确实没帮助"）。模型选型无意义（四个预训练模型全在 0.90–0.92）。
详见 `实验结果报告.html` 第六节。

### 6.3 指导书里 9 处照抄会报错的地方

工程包已全部修掉，明细见 `README.md` 第八节。最致命的三处：

| 位置 | 原文 | 后果 |
|---|---|---|
| 1.2 / 完整代码 | `class AlexNet(nn.Module)` | 缺冒号且类体没写，只是占位符 |
| 第三步 | `from step1_scratch import ResNet18Manual` | 悬空引用，那个类根本没定义 |
| 第三步 | `load_state_dict(official.state_dict(), strict=False)` | **strict=False 不豁免形状不匹配**，fc 512→2 vs 512→1000 必崩 |

### 6.4 首次运行不需要联网

4 个预训练权重已在 `torch_cache\hub\checkpoints\`，`config.py` 自动指向，
所以**第一次跑就能离线完成**（这是本项目相对原指导书的改进之一）。

### 6.5 搬迁说明

`data\` 和 `torch_cache\` 都在项目内、且 `config.py` 用绝对路径自动定位，
所以：

- **整个 `D:\CV\cat_dog` 文件夹可以拷到别的机器直接跑**（前提：对方装好 `dl` 那套依赖）
- 从任何目录执行脚本结果都一致（不依赖当前工作目录）
- 项目内**没有任何硬编码的绝对路径**

---

*本说明由环境实测 + 项目文件统计生成。环境信息采集时间：2026-09-14。*
